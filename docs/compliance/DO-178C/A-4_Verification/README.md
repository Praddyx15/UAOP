# A-4 — Verification

DO-178C Annex A Table A-4 artifacts: proof, not debugging — requirements-based testing, reviews, and analysis.

| Artifact | Where it lives | Notes |
|---|---|---|
| Test Cases & Procedures | `tests/` (`sitl/`, `integration/`, `e2e/`, `hil/`) + per-service `tests/` dirs | Layout: PROJECT_STRUCTURE.md §3; matrix: TESTING.md |
| Test Results | CI job output (`.github/workflows/pr.yml`, `nightly.yml`) + `ctest` reports | No separate archival system yet — CI run history is the record of truth today; a durable test-evidence archive is Phase 2+ scope |
| Review Records | Git PR review threads + `docs/00_Project_Foundation/DECISIONS.md` Review Log (the seven-lens critical review, run after every doc/code milestone — DOCUMENTATION_PROCESS.md) | The Review Log (Rn / Fn findings) is UAOP's actual review-record mechanism; it predates and is more rigorous than a bolted-on DO-178C review-log template |
| Traceability Matrix | [`../RTM.md`](../RTM.md) | Generated by `compliance/tools/rtm-generator/rtm_gen.py`; CI-gated (`--check` mode fails the build on an unknown requirement ID or a misplaced template placeholder) |

**Coverage target:** ≥ 20 HLRs carrying code annotations by the Phase 1 gate (ROADMAP.md) — tracked live in [`../RTM.md`](../RTM.md)'s coverage line.
